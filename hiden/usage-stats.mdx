## Get User Usage Stats

To see how much of the quota a user has consumed, execute the following:

```
weodata-admin user stats --uid=<uid>
```

<Note>
You should execute `weodata-admin user stats` with the `--sync-stats` option to receive the latest data.
</Note>